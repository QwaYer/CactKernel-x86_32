[package]
name = "cact_mm"
version = "0.1.0"
edition = "2021"

[dependencies]
cact_sync = { path = "../../sync" }

[lib]
crate-type = ["lib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 2
lto = true