[package]
name = "cact_crypto"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib"]

[dependencies]
# vendored rustls (patched for cact_shim)
rustls = { path = "vendor/rustls-0.23.40/rustls", default-features = false, features = ["hashbrown"] }

# pure-Rust no_std crypto
sha2 = { version = "0.10", default-features = false }
hmac = { version = "0.12", default-features = false }
hkdf = { version = "0.12", default-features = false }
chacha20 = { version = "0.9", default-features = false }
poly1305 = { version = "0.8", default-features = false }
aead = { version = "0.5", default-features = false }
aes-gcm = { version = "0.10", default-features = false, features = ["aes"] }
x25519-dalek = { version = "2", default-features = false, features = ["alloc", "precomputed-tables"] }
p256 = { version = "0.13", default-features = false, features = ["arithmetic", "ecdh"] }
elliptic-curve = { version = "0.13", default-features = false }
rand_core = { version = "0.6", default-features = false }
typenum = "1"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
codegen-units = 1

[profile.dev]
panic = "abort"
