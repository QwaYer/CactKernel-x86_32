[package]
name = "cact_shim"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib"]

[dependencies]

[profile.release]
panic = "abort"
opt-level = 2
lto = true
codegen-units = 1

[profile.dev]
panic = "abort"
